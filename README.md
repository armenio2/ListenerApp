# React Segment Event Listener

This is a React component that allows you to intercept click events throughout your application and send them to Segment, an analytics platform.

## Installation

To install React Segment Event Listener in your project, you can use npm or yarn:

```bash
npm install react-segment-event-listener
# or
yarn add react-segment-event-listener
